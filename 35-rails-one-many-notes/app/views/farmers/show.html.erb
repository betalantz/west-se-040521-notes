<h1>Farmer <%= @farmer.name %>'s Cows</h1>

<ul>
    <% @farmer.cows.each do |c| %>
        <li><%= c.name %></li>
    <% end %>
</ul>
<%= button_to "Delete", @farmer, method: :delete, data: {confirm: "Do you want to delete this farmer?"} %>